{
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names API,WEB --prefix-colors green,cyan \"cd mba_api && npm run dev\" \"cd mba_forms && npm run dev\""
  },
  "devDependencies": {
    "baseline-browser-mapping": "^2.10.43",
    "concurrently": "^9.2.1"
  }
}
